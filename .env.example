# Use one backend (or set TOCIFY_BACKEND=openai|cursor|gemini to force).

# OpenAI: easiest for most users — just set this and run.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Gemini: set API key and optional model.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Cursor CLI: backend requires `agent` on PATH and this key.
CURSOR_API_KEY=

# Optional: openai | cursor | gemini (default: auto from which key is set)
# TOCIFY_BACKEND=

# Runner weekly only: topic gardener is on by default; set to 0/false/no to disable.
# Runner AI steps (weekly/monthly/annual + gardener/redundancy) follow TOCIFY_BACKEND.
# TOPIC_GARDENER=0

# Weekly brief: include OpenAlex (scholarly works) and/or NewsAPI for each week's date range. Topic-derived search is used.
# OpenAlex: no API key required. Default on.
WEEKLY_OPENALEX=1
# News: set NEWS_BACKEND=newsapi and one of the keys below. Free tier = last 30 days only; for older weeks use NewsAPI paid or omit.
# NEWS_BACKEND=newsapi
# NEWS_API_KEY=
# NEWSAPI_API_KEY=
