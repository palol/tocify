# --- Backend / API keys ---
# Use one backend (or set TOCIFY_BACKEND=openai|cursor|gemini to force).

# OpenAI: easiest for most users — just set this and run.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Gemini: set API key and optional model.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Cursor CLI: backend requires `agent` on PATH and this key.
CURSOR_API_KEY=
# CURSOR_MODEL=unknown

# Optional: openai | cursor | gemini (default: auto from which key is set)
# TOCIFY_BACKEND=

# Runner weekly only: topic gardener is on by default; set to 0/false/no to disable.
# TOPIC_GARDENER=0

# --- Pipeline config (tocify/config.py) ---
# MAX_ITEMS_PER_FEED=50
# MAX_TOTAL_ITEMS=400
# LOOKBACK_DAYS=7
# SUMMARY_MAX_CHARS=500
# PREFILTER_KEEP_TOP=200
# BATCH_SIZE=50
# MIN_SCORE_READ=0.65
# MAX_RETURNED=40

# --- Cursor CLI & prompt / vault ---
# TOCIFY_CURSOR_TIMEOUT=600
# TOCIFY_CURSOR_RETRIES=2
# TOCIFY_PROMPT_PATH=prompt.txt
# TOCIFY_PROMPT_REF_MAX_FILES=200
# TOCIFY_PROMPT_REF_MAX_CHARS=1000000
# BCI_VAULT_ROOT=.

# --- News: NewsAPI ---
# Set NEWS_BACKEND=newsapi and one of the keys below. Free tier = last 30 days only.
# NEWS_BACKEND=newsapi
# NEWS_API_KEY=
# NEWSAPI_API_KEY=
# NEWS_API_TIMEOUT=30
# NEWS_API_PAGE_SIZE=100
# NEWS_API_MAX_ITEMS=200
# NEWS_API_DEFAULT_QUERY=news

# --- News: Google News ---
# ADD_GOOGLE_NEWS=0

# --- News: newsrooms (comma-separated URLs) ---
# NEWSROOMS_URLS=
# NEWSROOMS_TIMEOUT=20
# NEWSROOMS_MAX_ITEMS_PER_URL=30
# NEWSROOMS_MAX_ITEMS=100
# NEWSROOMS_DELAY_SECONDS=1.0

# --- EDGAR (comma-separated CIKs) ---
# EDGAR_CIKS=
# EDGAR_TIMEOUT=25
# EDGAR_MAX_ITEMS_PER_CIK=50
# EDGAR_MAX_ITEMS=200

# --- OpenAlex / historical ---
# OPENALEX_TIMEOUT=30
# OPENALEX_PAGE_SIZE=200
# OPENALEX_MAX_ITEMS_PER_RANGE=10000
# HISTORICAL_MAX_ITEMS=2000
# HISTORICAL_BACKENDS=openalex

# --- Clinical trials ---
# CLINICALTRIALS_QUERY=
# CLINICALTRIALS_TIMEOUT=30
# CLINICALTRIALS_PAGE_SIZE=100
# CLINICALTRIALS_MAX_ITEMS=200

# --- Google News (googlenews module) ---
# GOOGLE_NEWS_TIMEOUT=25
# GOOGLE_NEWS_MAX_ITEMS_PER_QUERY=30
# GOOGLE_NEWS_MAX_TOTAL_ITEMS=2000
# GOOGLE_NEWS_MAX_QUERIES=100

# --- Weekly brief ---
# OpenAlex: no API key required. Default on.
WEEKLY_OPENALEX=1
